% clc;clear;
% rng(1);
% n = 5;
% Y = complex(normrnd(0, 1, n, n*2), normrnd(0, 1, n, n*2));
% M = Y*Y';
% 
% % get maximum eigenvalue of M
% [U, Sigma, V] = svd(M);
% max_lambda = max(max(Sigma));
% 
% % MM algorithm
% k = 1;
% x =complex(normrnd(0, 1, n, 1), normrnd(0, 1, n, 1));
% x = x./abs(x);
% obj_value = [x'*M*x];
% while 1
%     x_ = x;
%     y = -(max_lambda*eye(n) - M)*x;
%     for i = 1 : n
%         x(i) = exp(angle(y(i))*1i);
%     end
%     
%     obj_value = [obj_value; x'*M*x];
%     k = k +1;
%     if abs(x_ - x) <= 10^(-2)
%         break
%     end
% end
% 
% plot(obj_value);
% 
R