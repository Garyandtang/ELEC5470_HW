function [] = noise_cov(x)

mean_x = mean(x)
x_ = x - mean_x;
for j = 1:3
    temp = 0
    n = 0
    for i = 1:size(x,2)
        if abs(x(i,j) - mean_x(j)
        
        
    end
end




cov = x_'*x_

figure
subplot(3,1,1)
plot(x(:,1))
hold on 
plot(mean_x(1)*ones(1,size(x,1)))
subplot(3,1,2)
plot(x(:,2))
subplot(3,1,3)
plot(x(:,3)*180/pi)
